/*

import java.util.ArrayList;

public class Test2 {

    public static void main(String[] args) throws PersonNotFoundException {
        /*
        int popolazione = 100;
        int largezza = 8;
        int altezza = 5;
        ArrayList<Persona> persone = new ArrayList<>(popolazione);

        for (int i = 0; i < popolazione; i++) {
            persone.add(new Persona(i, new Governo()));
        }

        Arena arena = new Arena(altezza, largezza);

        arena.distribuisciPersone(persone);

        for (Persona persona : persone) {
            System.out.println("La persona con ID = " + persona.getID() + " si trova in posizione " + "(" +  persona.getPosizione().getY() + "," + persona.getPosizione().getX() + ")");
        }

        arena.move(persone);


        System.out.println("Qui faccio il movimento delle persone ");


        for (Persona persona : persone) {
            System.out.println("La persona con ID = " + persona.getID() + " si trova in posizione " + "(" +  persona.getPosizione().getY() + "," + persona.getPosizione().getX() + ")");
        }


        Virus.setI(100);

        Persona persona_infetta = persone.get(0);
        persona_infetta.setVir(new Virus());
        persona_infetta.setStato(StatoSalute.ROSSO);
        System.out.println(persona_infetta.getStato() + ", " + persona_infetta.getID());

        arena.check_incontri();

        for (Persona persona : persone) {
            System.out.println("La persona con ID = " + persona.getID() + " si trova in posizione " + "(" +  persona.getPosizione().getY() + "," + persona.getPosizione().getX() + ") e virus è settato  a " + persona.getVir() + " ed ha stato " + persona.getStato());
        }
*/


    }
}*/
