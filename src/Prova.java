public class Prova {

	static int I;

	public static void main(String[] args) throws PersonNotFoundException{
		/*
		I = Integer.parseInt(args[0]);
		
		Arena arena = new Arena(5,5);
		
		if (arena.matrice[0][0] == null) {
			System.out.println("ciao deficiente");
		}
		
		arena.init(5);
		for (int i = 0; i < 5; i++) {
			for (int j = 0; j < 5; j++) {
				if (arena.matrice[i][j].size() == 1) {
					System.out.println(i + " " + j + "ID " + arena.matrice[i][j].fila.get(0).ID);
					//System.out.println(arena.matrice[i][j].fila.get(0).stato);
				}
			}
		}
		System.out.println("Faccio il primo movimento");
		arena.move();
		for (int i = 0; i < 5; i++) {
			for (int j = 0; j < 5; j++) {
				if (arena.matrice[i][j].size() != 0) {
					for (int k = 0; k < arena.matrice[i][j].size(); k++){
					System.out.println(i + " " + j + "ID " + arena.matrice[i][j].fila.get(k).ID);
					}
				}
			}
		}
		*/
	}	
}
